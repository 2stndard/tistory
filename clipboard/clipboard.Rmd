---
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## 클립보드 데이터 읽기, 쓰기 

R에서 데이터를 분석하기 위해서 가장 먼저 해야할 일은 가지고 있는 데이터를 R로 불러들이는 작업이다. 또 많이 하는 작업은 R에서 분석한 데이터를 외부에서 사용할 수 있도록 파일로 정리하는 작업이다. 

본 포스트에서는 R에서 데이터를 불러 읽어들이거나 외부 프로그램에서 사용할 수 있도록 클립보드(clipboard)로 데이터를 넣거나(ctrl+c) 클립보드의 데이터를 읽어오는(ctrl+v) 방법을 설명하고자 한다. 

단 이 포스트에서는 windows 운영체제에서 사용하는 방법에 한한다.(필자는 iOS를 매우 매우 싫어한다.)

이 포스트에서 사용하는 데이터는 `gapminder` 패키지의 gapminder 데이터프레임을 사용한다. 

```{r gapminder}
library(gapminder)
```

### 클립보드로 데이터 내보내기

R에서 생성된 데이터를 외부 프로그램에서 사용하기 위해서 클립보드로 데이터를 붙여넣기(ctrl+v) 위해서는 `readxl` 패키지 함수를 사용하는 방법과 `clipr` 패키지를 사용하는 방법의 두가지를 설명한다. 

#### `readxl` 패키지

R에서 생성된 데이터를 컴퓨터 메모리의 클립보드(clipboard)로 내보내면 MS Excel이나 다른 많은 프로그램에서 사용할 수 있다. `readxl` 패키지에는 R의 데이터를 MS Excel이나 CSV(comma seperated values)로 내보낼때 사용하는 함수인 `write.csv()`, `write.excel()`, `write.table()` 등을 제공한다. 이 함수들에서는 


사용자들이 데이터를 저장하는 파일들은 여러가지가 있지만 많이 사용되는 파일 포맷은 xls, xlsx(Microsoft의 Excel)과 csv(comma seperated values)가 있다. R에서 생산된 데이터를 이 파일로 저장된 데이터를 R로 불러들이는데 

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
